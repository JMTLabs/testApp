<p id="notice"><%= notice %></p>
<style>
.button {
  background-color: #4CAF50;
  border: none;
  color: white;
  padding: 15px 32px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
}
</style>

<button class="button">Lets Grab Something Else</button>

<%= render 'form', project: @project %>

<%= button_to "We are eating here now!", @project, :method => :get %>
<%= button_to "Back", projects_path, :method => :get %>
